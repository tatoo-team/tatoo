package fr.umlv.tatoo.runtime.buffer;

/**
 * A <code>TokenBuffer</code> which tokens are CharSequences.
 * 
 * @see TokenBuffer
 */
public interface CharSequenceTokenBuffer extends TokenBuffer<CharSequence> {
  // reified interface
}
